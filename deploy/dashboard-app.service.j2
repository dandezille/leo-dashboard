[Unit]
Description=Dashboard server
Requires=network.target

[Service]
Type=simple
User=pi
Group=pi
WorkingDirectory={{dashboard_directory}}
ExecStart=/bin/bash {{dashboard_directory}}/bin/start
Restart=on-abort

[Install]
WantedBy=multi-user.target
